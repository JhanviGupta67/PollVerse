<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
     https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context 
        https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 1. Component Scan: Scans Service and DAO packages for @Service and @Repository -->
    <context:component-scan base-package="com.pollverse.service, com.pollverse.dao" />

    <!-- 2. Database Configuration (FIXED FOR MYSQL) -->
    <bean class="org.springframework.jdbc.datasource.DriverManagerDataSource" name="ds">
        <!-- **CRITICAL FIX:** Using the correct MySQL Driver -->
        <property name="driverClassName" value="com.mysql.cj.jdbc.Driver"/>
        
        <!-- Standard MySQL URL. Make sure the port (3306) and database name (pollversedb) are correct. -->
        <property name="url" value="jdbc:mysql://localhost:3306/pollversedb?createDatabaseIfNotExist=true"/>
        
        <!-- **UPDATE THESE** to match your MySQL user/pass -->
        <property name="username" value="root"/> 
        <property name="password" value="Jenny@1223"/> 
    </bean>
    
    <!-- 3. JdbcTemplate: Used by the DAO classes for database operations -->
    <bean class="org.springframework.jdbc.core.JdbcTemplate" name="jdbcTemplate">
        <property name="dataSource" ref="ds"/>
    </bean>
    
    <!-- 4. BCrypt Password Encoder for UserService (Security) -->
    <!-- Note: You must have the spring-security-core dependency for this class. -->
    <bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />

</beans>
