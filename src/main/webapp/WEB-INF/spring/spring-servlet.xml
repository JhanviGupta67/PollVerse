<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:mvc="http://www.springframework.org/schema/mvc"
    xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 1. Component Scan: ONLY scans the controller package for @Controller. 
         It avoids rescanning the Service/DAO packages already loaded by the Root Context. -->
    <context:component-scan base-package="com.pollverse.controller" />
    
    <!-- 2. Enables Spring MVC annotations (like @Controller, @RequestMapping, etc.) 
         This line is necessary to process your @GetMapping and @PostMapping annotations. -->
    <mvc:annotation-driven /> 

    <!-- 3. View Resolver: Maps logical view names (like "login" or "pollList") 
         to physical JSP files located in /WEB-INF/views/ -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver" name="viewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>

</beans>
